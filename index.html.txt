<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RxDecode AI | Precision Medical Decryption</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap');

        :root {
            --bg-color: #0d1117;
            --text-color: #c9d1d9;
            --accent-color: #00f0ff;
            --card-bg: #161b22;
        }

        body, html {
            margin: 0;
            padding: 0;
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            line-height: 1.6;
            scroll-behavior: smooth;
        }

        /* Navigation */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1.5rem 5%;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }

        .logo {
            font-weight: 800;
            font-size: 1.5rem;
            color: #fff;
            letter-spacing: -0.5px;
        }

        .logo span {
            color: var(--accent-color);
        }

        .nav-button {
            background-color: transparent;
            color: #fff;
            border: 1px solid var(--accent-color);
            padding: 0.5rem 1.2rem;
            border-radius: 4px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .nav-button:hover {
            background-color: var(--accent-color);
            color: #000;
        }

        /* Hero Section */
        header {
            text-align: center;
            padding: 6rem 5% 4rem;
            max-width: 800px;
            margin: 0 auto;
        }

        h1 {
            font-size: 3.5rem;
            font-weight: 800;
            color: #fff;
            line-height: 1.2;
            margin-bottom: 1.5rem;
            letter-spacing: -1px;
        }

        h1 span {
            background: -webkit-linear-gradient(45deg, var(--accent-color), #0077ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .subtitle {
            font-size: 1.2rem;
            margin-bottom: 2.5rem;
            color: #8b949e;
        }

        .primary-cta {
            background-color: var(--accent-color);
            color: #000;
            padding: 1rem 2rem;
            border-radius: 4px;
            text-decoration: none;
            font-weight: 800;
            font-size: 1.1rem;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
            display: inline-block;
            border: none;
            cursor: pointer;
        }

        .primary-cta:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(0, 240, 255, 0.4);
        }

        /* Features Section */
        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            padding: 4rem 5%;
            max-width: 1200px;
            margin: 0 auto;
        }

        .feature-card {
            background-color: var(--card-bg);
            padding: 2rem;
            border-radius: 8px;
            border: 1px solid rgba(255,255,255,0.05);
            transition: transform 0.3s ease;
        }

        .feature-card:hover {
            transform: translateY(-5px);
            border-color: rgba(0, 240, 255, 0.3);
        }

        .feature-card h3 {
            color: #fff;
            margin-top: 0;
            font-size: 1.3rem;
        }

        .feature-icon {
            font-size: 2rem;
            margin-bottom: 1rem;
        }

        /* Footer / Waitlist */
        footer {
            text-align: center;
            padding: 4rem 5%;
            background-color: #090c10;
            border-top: 1px solid rgba(255,255,255,0.1);
        }

        .waitlist-form {
            max-width: 400px;
            margin: 0 auto;
            display: flex;
            gap: 10px;
        }

        .waitlist-input {
            flex-grow: 1;
            padding: 0.8rem;
            border-radius: 4px;
            border: 1px solid rgba(255,255,255,0.2);
            background: transparent;
            color: #fff;
        }

        .waitlist-input:focus {
            outline: none;
            border-color: var(--accent-color);
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">RxDecode<span>.ai</span></div>
        <a href="#waitlist" class="nav-button">Join Beta</a>
    </nav>

    <header>
        <h1>Decoding the Unreadable. <span>Instantly.</span></h1>
        <p class="subtitle">The first proprietary Vision-Language AI fine-tuned specifically for localized medical prescriptions, doctor shorthand, and automated DDI safety checks.</p>
        <a href="#waitlist" class="primary-cta">Request Early Access</a>
    </header>

    <section class="features">
        <div class="feature-card">
            <div class="feature-icon">👁️</div>
            <h3>Specialized Vision Encoding</h3>
            <p>Standard OCR fails on doctor shorthand. Our model is fine-tuned on thousands of localized, verified prescriptions to recognize specific structural strokes, not just letters.</p>
        </div>
        <div class="feature-card">
            <div class="feature-icon">⚡</div>
            <h3>Sub-Second Telegram Integration</h3>
            <p>Designed for fast-paced pharmacy environments. Snap a photo via our Telegram Bot integration and receive perfectly structured JSON extraction and pricing in under 2 seconds.</p>
        </div>
        <div class="feature-card">
            <div class="feature-icon">🛡️</div>
            <h3>Automated Safety Layers</h3>
            <p>Beyond transcription. The engine automatically cross-references identified generics for Drug-Drug Interactions (DDIs) and verifies if dosages fall within standard therapeutic ranges.</p>
        </div>
    </section>

    <footer id="waitlist">
        <h2>Join the Closed Beta</h2>
        <p style="margin-bottom: 2rem; color: #8b949e;">We are currently onboarding selected pharmacies and clinics to train our next-generation model.</p>
        <form class="waitlist-form" onsubmit="alert('Thank you! We will be in touch when a beta slot opens.'); return false;">
            <input type="email" class="waitlist-input" placeholder="Enter your business email" required>
            <button type="submit" class="primary-cta" style="padding: 0.8rem 1.5rem; font-size: 1rem;">Join Waitlist</button>
        </form>
        <p style="margin-top: 3rem; font-size: 0.8rem; color: #484f58;">© 2026 RxDecode AI. All rights reserved.</p>
    </footer>

</body>
</html>